services:
  - type: web
    name: telegram-discord-bot
    runtime: python311
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: PORT
        value: 5000
